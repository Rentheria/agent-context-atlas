---
id: role-operator
kind: role
title: Role Operator
---

# role-operator

Rol sintético relacionado (`related`) con `host-demo-01`. Revisa fichas; no tiene métricas de cupo.

Llega desde `role-coordinator` (`leads_to`).
