---
id: role-coordinator
kind: role
title: Role Coordinator
---

# role-coordinator

Rol sintético que **owns** `bot-alpha` y `bot-beta`. Coordina `org-example`.

`leads_to` `role-operator`. No publica métricas de cupo ni de hardware.
